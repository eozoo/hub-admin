<?xml version="1.0" encoding="UTF-8"?>
<!--

    Licensed under the Apache License, Version 2.0 (the "License");
    you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0.txt

    Unless required by applicable law or agreed to in writing,
    software distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and limitations under the License.

-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cowave.hub.admin.infra.rbac.mapper.SysDeptMapper">
    <sql id="deptColumns">
        d.dept_id,
        d.dept_code,
        d.dept_name,
        d.dept_short,
        d.dept_addr,
        d.dept_phone,
        d.create_by,
        d.create_time,
        d.update_by,
        d.update_time,
        d.remark
    </sql>

    <select id="getPrimaryDeptByUserId" resultType="com.cowave.hub.admin.domain.rbac.entity.SysDept">
        select
        <include refid="deptColumns"/>
        from sys_user_dept ud, sys_dept d
        where d.dept_id = ud.dept_id and ud.user_id = #{userId}
        order by ud.is_default desc limit 1
    </select>

    <resultMap id="deptMap" type="com.cowave.hub.admin.domain.rbac.entity.SysDept">
        <result column="dept_id"       property="deptId"/>
        <result column="dept_code"     property="deptCode"/>
        <result column="dept_name"     property="deptName"/>
        <result column="dept_short"    property="deptShort"/>
        <result column="dept_addr"     property="deptAddr"/>
        <result column="dept_phone"    property="deptPhone"/>
        <result column="remark"        property="remark"/>
        <result column="create_by"     property="createBy"/>
        <result column="create_time"   property="createTime"/>
        <result column="update_by"     property="updateBy"/>
        <result column="update_time"   property="updateTime"/>
    </resultMap>

    <resultMap id="deptListDtoMap" type="com.cowave.hub.admin.domain.rbac.entity.pto.DeptListPto" extends="deptMap">
        <result column="parent_id"    property="parentId"/>
        <collection property="leaderList" javaType="list" ofType="com.cowave.hub.admin.domain.rbac.entity.pto.UserNamePto">
            <result column="user_id"      property="userId"/>
            <result column="user_name"    property="userName"/>
        </collection>
    </resultMap>

    <select id="queryDeptListDpo" resultMap="deptListDtoMap">
        select t.parent_id, u.user_id, u.user_name,
        <include refid="deptColumns"/>
        from sys_dept d
        left join sys_dept_diagram t on d.dept_id = t.dept_id
        left join sys_user_dept ud on d.dept_id = ud.dept_id and ud.is_leader = 1
        left join sys_user u on u.user_id = ud.user_id
        where d.tenant_id = #{tenantId}
        <if test="query.deptName != null and query.deptName != ''">
            and dept_name like concat('%', #{query.deptName}, '%')
        </if>
        <if test="query.deptPhone != null and query.deptPhone != ''">
            and dept_phone like concat('%', #{query.deptPhone}, '%')
        </if>
        order by t.parent_id, t.dept_id
    </select>

    <resultMap id="deptInfoDtoMap" type="com.cowave.hub.admin.domain.rbac.entity.pto.DeptInfoPto" extends="deptMap">
        <collection property="parentIds" javaType="list" ofType="Long">
            <result column="parent_id"/>
        </collection>
        <collection property="leaderIds" javaType="list" ofType="Long">
            <result column="user_id"/>
        </collection>
    </resultMap>

    <select id="queryDeptInfoDpo" resultMap="deptInfoDtoMap">
        select t.parent_id, u.user_id,
        <include refid="deptColumns"/>
        from sys_dept d
        left join sys_dept_diagram t on d.dept_id = t.dept_id
        left join sys_user_dept u on d.dept_id = u.dept_id and u.is_leader = 1
        where t.tenant_id = #{tenantId} and d.dept_id = #{deptId}
    </select>
</mapper>
