<?xml version="1.0" encoding="UTF-8" ?>
<!--

    Licensed under the Apache License, Version 2.0 (the "License");
    you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0.txt

    Unless required by applicable law or agreed to in writing,
    software distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and limitations under the License.

-->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cowave.hub.admin.infra.flow.mapper.FlowLeaveMapper">

    <resultMap id="LeavePtoMap" type="com.cowave.hub.admin.domain.flow.entity.pto.LeaveInfoPto">
        <result property="id"            column="id"           />
        <result property="leaveType"     column="leave_type"   />
        <result property="reason"        column="reason"       />
        <result property="beginTime"     column="begin_time"   />
        <result property="endTime"       column="end_time"     />
        <result property="applyUser"     column="apply_user"   />
        <result property="applyUserName" column="apply_user_name" />
        <result property="applyTime"     column="apply_time"   />
        <result property="processId"     column="process_id"   />
        <result property="processStatus" column="process_status" />
    </resultMap>

    <sql id="LeaveField">
        l.id,
        l.leave_type,
        l.reason,
        l.begin_time,
        l.end_time,
        l.apply_user,
        l.apply_time,
        l.process_id,
        l.process_status
    </sql>

    <select id="list" resultMap="LeavePtoMap">
        select
        u.user_name apply_user_name,
        <include refid="LeaveField"/>
        from flow_leave l
        left join hub_user u on l.apply_user = u.user_code
        <where>
            <if test="leave.applyUser != null">
                and l.apply_user = #{leave.applyUser}
            </if>
            <if test="leave.leaveType != null">
                and l.leave_type = #{leave.leaveType}
            </if>
            <if test="leave.processStatus != null">
                and l.process_status = #{leave.processStatus}
            </if>
            <if test="leave.beginTime != null">
                <![CDATA[
                and l.end_time >= #{leave.beginTime}
                ]]>
            </if>
            <if test="leave.endTime != null">
                <![CDATA[
                and l.begin_time <= #{leave.endTime}
                ]]>
            </if>
        </where>
    </select>

    <select id="info" resultMap="LeavePtoMap">
        select
        u.user_name apply_user_name,
        <include refid="LeaveField"/>
        from flow_leave l
        left join hub_user u on l.apply_user = u.user_code
        where id = #{id}
    </select>
</mapper>
